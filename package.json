{
  "scripts": {
    "test": "jest",
    "lint": "eslint"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "jest": {
    "projects": ["packages/*"],
    "preset": "./jest-defaults.json",
    "reporters": [
        "default",
        "jest-screenshot/reporter"
    ]
  },
  "devDependencies": {
    "jest": "^25.1.0",
    "lerna": "^3.2.1"
  }
}
